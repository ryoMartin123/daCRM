import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Hide the floating Next.js dev indicator — it otherwise shows up inside the
  // mobile preview frame. Dev-only; no effect on production.
  devIndicators: false,
};

export default nextConfig;
